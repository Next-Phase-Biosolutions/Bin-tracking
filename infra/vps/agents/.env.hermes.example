# Copy to /opt/bin-tracker/infra/vps/agents/.env.hermes on the VPS. chmod 600.
# One file per agent — a leaked key exposes only that agent. See kvmplan.md §11.
ANTHROPIC_API_KEY=""
REDIS_URL="redis://agents_redis:6379"
